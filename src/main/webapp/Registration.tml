<html t:type="layout" title="Registration"
      xmlns:t="http://tapestry.apache.org/schema/tapestry_5_1_0.xsd"
      xmlns:p="tapestry:parameter">
<head></head><body>
    Registration:
    <br/><br/>
    <t:form t:id="registrationForm">
    <t:errors/>
        First Name:<br/>
        <div><t:textfield t:id="firstname" t:value="user.firstName"
        t:validate="required,minlength=2,regexp=^[A-Z][a-z]+$"/></div>

        Last Name:<br/>
        <div><t:textfield t:id="lastname" t:value="user.lastName"
        t:validate="required,minlength=2,regexp=^[A-Z][a-z]+$"/></div>

        Personal Number:<br/>
        <div><t:textfield t:id="personalnumber" t:value="user.personalNumber"
        t:validate="required,minlength=2"/></div>

        User Name:<br/>
        <div><t:textfield t:id="username" t:value="user.userName"
        t:validate="required,minlength=5"/></div>

        Password:<br/>
        <div><t:passwordfield t:id="password" t:value="user.password"
        t:validate="required,minlength=5"/></div>

        Retype Password:<br/>
        <div><t:passwordfield t:id="password2" t:value="password2"/></div>

        <br/>
        <input id="submit" type="submit" value="Submit"/>
    </t:form>
    <br/>
    <div>${name}</div>
    <t:pagelink page="Index">Back To The Hello Page</t:pagelink>
</body>
</html>
